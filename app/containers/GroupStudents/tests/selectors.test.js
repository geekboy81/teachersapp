// import { selectGroupStudentsDomain } from '../selectors';

describe('selectGroupStudentsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
