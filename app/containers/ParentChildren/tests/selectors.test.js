// import { selectParentChildrenDomain } from '../selectors';

describe('selectParentChildrenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
