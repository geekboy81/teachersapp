// import { selectGroupDetailsDomain } from '../selectors';

describe('selectGroupDetailsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
