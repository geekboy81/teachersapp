// import { selectModulesHighlightsDomain } from '../selectors';

describe('selectModulesHighlightsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
